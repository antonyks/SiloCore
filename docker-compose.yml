services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: silocore-backend
    ports:
      - "5000:5000"
    env_file:
      - ./backend/.env
    environment:
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/silocore
      OLLAMA_HOST: http://host.docker.internal:11434
    volumes:
      - ./backend:/app
      - /app/node_modules
    depends_on:
      postgres:
        condition: service_healthy
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - silocore-network

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: silocore-frontend
    ports:
      - "5173:5173"
    env_file:
      - ./frontend/.env
    volumes:
      - ./frontend:/app
      - /app/node_modules
    depends_on:
      - backend
    networks:
      - silocore-network

  postgres:
    image: postgres:16
    container_name: silocore-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: silocore
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d silocore"]
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - silocore-network

networks:
  silocore-network:
    driver: bridge

volumes:
  pgdata:
