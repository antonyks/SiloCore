meta {
  name: Chat Session Delete
  type: http
  seq: 17
}

delete {
  url: {{host}}/api/chat/{{sessionId}}
  body: none
  auth: inherit
}

headers {
  Authorization: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}

