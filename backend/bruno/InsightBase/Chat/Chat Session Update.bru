meta {
  name: Chat Session Update
  type: http
  seq: 16
}

put {
  url: {{host}}/api/chat/{{sessionId}}
  body: json
  auth: inherit
}

headers {
  Authorization: {{token}}
}

body:json {
  {
    "title": "Updated Test Chat Session"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
